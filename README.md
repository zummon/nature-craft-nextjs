Change the header and footer background in `./app/layout.jsx`

Add, edit and remove your contents in `./app/blog/[slug]/*.md` to generate the site pages.

Something breaks, needs upgrade. Let me know.
